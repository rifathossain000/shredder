# shredder
This is a file content remover. Generally when people want to keep his original file but want to remove the file content inside it then they go inside and remove all content by select the text and press backspace. But using this they just type the file name and 'BOOM'. There file content will remove but the actuall file still exists on the folder.
